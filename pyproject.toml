[tool.black]
line-length = 100
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 100

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [".venv", "venv", "data/external", "data/processed"]

[tool.nbqa.addopts]
black = ["--line-length=100"]
